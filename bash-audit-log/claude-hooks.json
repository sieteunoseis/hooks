{
  "name": "bash-audit-log",
  "description": "Log all bash commands Claude runs to an audit file",
  "author": "sieteunoseis",
  "version": "1.0.0",
  "hooks": [
    {
      "name": "Log bash commands",
      "description": "Appends every bash command to ~/.claude/bash-log.txt with timestamps",
      "default": true,
      "event": "PreToolUse",
      "matcher": "Bash",
      "hook": {
        "type": "command",
        "command": "jq -r '.tool_input.command' | { read -r cmd; echo \"$(date -u +%Y-%m-%dT%H:%M:%SZ) $cmd\" >> ~/.claude/bash-log.txt; }"
      }
    }
  ]
}
