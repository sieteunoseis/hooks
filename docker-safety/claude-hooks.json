{
  "name": "docker-safety",
  "description": "Block destructive Docker commands — system prune, force remove, and volume deletion",
  "author": "sieteunoseis",
  "version": "1.0.0",
  "hooks": [
    {
      "name": "Block Docker prune and force remove",
      "description": "Blocks docker system prune, docker rm -f, docker volume rm, and docker compose down -v",
      "default": true,
      "event": "PreToolUse",
      "matcher": "Bash",
      "hook": {
        "type": "command",
        "command": "jq -r '.tool_input.command' | { read -r cmd; if echo \"$cmd\" | grep -qE '(docker system prune|docker (rm|rmi) -f|docker volume (rm|prune)|docker compose down -v)'; then echo '{\"decision\":\"block\",\"reason\":\"BLOCKED: destructive Docker operation. This can remove containers, images, or volumes.\"}'; fi; }"
      }
    }
  ]
}
